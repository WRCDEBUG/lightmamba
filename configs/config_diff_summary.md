
# Release Config Adaptation Summary

Original configs were not modified. Release configs replace local absolute paths with placeholders:

- `DATA_ROOT`
- `CHECKPOINT_ROOT`
- `OUTPUT_ROOT`

Checkpoint and detector files are not included. Frozen SHA256 values are preserved for verification.
